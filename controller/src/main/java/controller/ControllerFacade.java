package controller;import model.*;import model.dao.MapDAO;import view.IView;import java.io.IOException;import java.sql.SQLException;/** * <h1>The Class ControllerFacade provides a facade of the Controller component.</h1> * * @author Jean-Aymeric DIET jadiet@cesi.fr * @version 1.0 */public class ControllerFacade{    /**     * The view.     */    private final IView view;    /**     * The model.     */    private final IModel model;    private MapDAO map = new MapDAO();    private SpriteSheet sprite;    private MapDAO niveau = new MapDAO();    /**     * Instantiates a new controller facade.     *     * @param view  the view     * @param model the model     */    public ControllerFacade(final IView view, final IModel model) throws IOException {        super();        this.view = view;        this.model = model;        sprite = new SpriteSheet();    }    /**     * Start.     *     * @throws SQLException the SQL exception     */    public void start() throws SQLException, IOException {        String str_map;        char tab_map[][];        str_map = niveau.readMap("map1");        int map_Width = map.readSize("map1", "width");        int map_Heigth = map.readSize("map1", "heigth");        tab_map = map.putMapInTable(str_map);        Assets assets = new Assets();            for (int y = 0; y < tab_map[y].length; y++) {                for (int x = 0; x < tab_map.length; x++) {                    int block = (int) tab_map[x][y] - 48;                    int i = x*16;                    int j = y*16;                    switch (block) {                        case Dirt.REF:                            view.drawImage(assets.dirt, i, j);                            break;                        case Wall.REF:                            view.drawImage(assets.wall, i, j);                            break;                        case Empty.REF:                            view.drawImage(assets.empty, i, j);                            break;                        case BWall.REF:                            view.drawImage(assets.bwall, i, j);                            break;                        case Diamond.REF:                            view.drawImage(assets.diamond, i, j);                            break;                        case Rock.REF:                            view.drawImage(assets.rock, i, j);                            break;                        case Exit.REF:                            view.drawImage(assets.exit, i, j);                            break;                    }                    //System.out.print(tab_map[x][y]);                    try {                        Thread.sleep(5);                    } catch (InterruptedException e) {                        e.printStackTrace();                    }                }               //System.out.println("");            }        //}    }    /**     * Gets the view.     *     * @return the view     */    public IView getView() {        return this.view;    }    /**     * Gets the model.     *     * @return the model     */    public IModel getModel() {        return this.model;    }}